const splashScreen = {
    name: "Shadows of Mordor"
};

/**
 * TODO: Display only for a few seconds and then replace with
 * @type {*|jQuery.fn.init|jQuery|HTMLElement}
 */
const SplashScreen = $(
    `<div class="welcome-screen screen centered">
        <div class="welcome-screen__content">
            <h1 class="header__title">${splashScreen.name}</h1>
        </div>
    </div>`
);
const currentScreen = $(".current-screen");

/**
 * TODO: Set current screen to this screen
 * @param screen
 */
const setScreen = screen => {

};

$(document).ready(() => {
    currentScreen.replaceWith(SplashScreen);
});

// export default SplashScreen;

// travis ci
cache:
  yarn: true
  directories:
    - node_modules
script:
  - npm test
before_deploy:
  - npm run build
deploy:
  provider: s3
  access_key_id: "some"
  secret_access_key: "super secret"
  bucket: "secret_bucket"
  skip_cleanup: true
  local_dir: dist
on:
  branch: "master"


deployment:

Elastic Beanstalk
Commands:
start
prestart:
poststart


uglify JavaScript

ssh passphrase is mordor:

elastic beanstalk deployment error:
No permissins to create a role. Create an IAM called elasticbeanstalk-service-role with appropriate permissions.

    // main scene
    // consider a general constructor for scenes?
    // pass array of children which are DOM elements?
    // communication between components via events!


        // switching between scenes?
        // time, tap, events!
        // let currentScreen = $("#currentScreen");
        // currentScreen.append(Splash(config.title));
        // currentScreen.append(Menu(config.menu));

        // change after some seconds
        // currentScreen.on();
        // execute game loop when game play starts?
        // MainLoop();

        // support touch, mouse and keyboard input