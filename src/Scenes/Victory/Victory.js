import $ from "jquery";

const Menu = ({ Play, Instructions, Settings}) => {
   console.log(Play);
   console.log(Instructions);
   console.log(Settings);
   return (
     `<h1>Menu!</h1>`
   );
};

export default  Menu;